# demo
This is a simple python Hello World App that splashes up a graphic that can be toggled.  It also shows the host name that is running the app.  When running as a container the host name is generated dynamically.  
